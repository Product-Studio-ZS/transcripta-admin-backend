PORT=9002
DB_HOST=mysql
DB_USER=admin_backend
DB_PASSWORD=
DB_NAME=transcripta
JWT_SECRET=
CORS_ORIGIN=https://admin.transcripta.ru
TELEGRAM_BOT_TOKEN=

# Internal URL основного backend (для admin-retry через async pipeline, #407)
BACKEND_INTERNAL_URL=http://127.0.0.1:9000
# Worker claim secret — должен совпадать с WORKER_CLAIM_SECRET основного backend
WORKER_CLAIM_SECRET=dev-secret
